import type { VibeProofReceipt } from "./types.js";
function esc(value: string): string { return value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;"); }
function md(value: string): string { return value.replaceAll("|", "\\|").replaceAll("\n", " "); }
export function renderMarkdown(receipt: VibeProofReceipt): string { const lines = [`# VibeProof — ${receipt.projectName}`, "", `**Result:** ${receipt.result.status.toUpperCase()}`, `**Commit:** \`${receipt.source.commitSha}\``, `**Clean clone:** ${receipt.source.cleanClone ? "yes" : "no"}`, `**Receipt hash:** \`${receipt.receiptHash}\``, "", "## Proof ladder", "", "| Stage | Required | Status | Evidence | Error |", "|---|---:|---|---|---|"]; for (const stage of receipt.stages) lines.push(`| ${stage.name} | ${stage.required ? "yes" : "no"} | ${stage.status} | ${md(stage.evidence.join("; "))} | ${md(stage.error?.message ?? "")} |`); lines.push("", "## Claims", ""); for (const [name, value] of Object.entries(receipt.claims)) lines.push(`- **${name}:** ${String(value)}`); lines.push("", "## Evidence artifacts", ""); if (receipt.artifacts.length === 0) lines.push("No evidence artifacts were recorded."); else { lines.push("| Path | Media type | Bytes | SHA-256 |", "|---|---|---:|---|"); for (const artifact of receipt.artifacts) lines.push(`| ${md(artifact.path)} | ${artifact.mediaType} | ${artifact.bytes} | \`${artifact.sha256}\` |`); } return `${lines.join("\n")}\n`; }
export function renderHtml(receipt: VibeProofReceipt): string { const rows = receipt.stages.map((item) => `<tr><td>${esc(item.name)}</td><td>${item.required ? "yes" : "no"}</td><td class="${item.status}">${esc(item.status)}</td><td>${esc(item.evidence.join("; "))}</td><td>${esc(item.error?.message ?? "")}</td></tr>`).join(""); const artifacts = receipt.artifacts.map((item) => `<tr><td>${esc(item.path)}</td><td>${esc(item.mediaType)}</td><td>${item.bytes}</td><td><code>${esc(item.sha256)}</code></td></tr>`).join(""); return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>VibeProof ${esc(receipt.projectName)}</title><style>:root{color-scheme:dark}body{font:15px/1.55 system-ui;margin:0;background:#070b16;color:#e8eefc}.wrap{max-width:1120px;margin:auto;padding:32px}.card{background:#111827;border:1px solid #293553;border-radius:16px;padding:24px;margin:16px 0}.verified,.passed{color:#4ade80}.failed{color:#fb7185}.skipped{color:#fbbf24}table{border-collapse:collapse;width:100%}td,th{border-bottom:1px solid #293553;padding:10px;text-align:left;vertical-align:top;word-break:break-word}code{word-break:break-all;color:#bfdbfe}</style></head><body><main class="wrap"><section class="card"><h1>VibeProof</h1><p class="${receipt.result.status}"><strong>${esc(receipt.result.status.toUpperCase())}</strong></p><p>${esc(receipt.projectName)}</p><p><code>${esc(receipt.source.commitSha)}</code></p><p><strong>Receipt:</strong> <code>${esc(receipt.receiptHash)}</code></p></section><section class="card"><h2>Proof ladder</h2><table><thead><tr><th>Stage</th><th>Required</th><th>Status</th><th>Evidence</th><th>Error</th></tr></thead><tbody>${rows}</tbody></table></section><section class="card"><h2>Evidence artifacts</h2><table><thead><tr><th>Path</th><th>Media</th><th>Bytes</th><th>SHA-256</th></tr></thead><tbody>${artifacts}</tbody></table></section></main></body></html>`; }
export const renderMarkdownReport = renderMarkdown; export const renderHtmlReport = renderHtml;
