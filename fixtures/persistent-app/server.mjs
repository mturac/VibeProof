import { createServer } from "node:http";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { dirname, resolve } from "node:path";
const port=Number(process.env.PORT??3000);const persistence=process.env.PERSISTENCE!=="disabled";const statePath=resolve(process.env.STATE_FILE??"data/state.json");let memory={title:""};
async function readState(){if(!persistence)return memory;try{return JSON.parse(await readFile(statePath,"utf8"))}catch(error){if(error&&typeof error==="object"&&error.code==="ENOENT")return {title:""};throw error}}
async function saveState(state){if(!persistence){memory=state;return}await mkdir(dirname(statePath),{recursive:true});await writeFile(statePath,`${JSON.stringify(state)}\n`,"utf8")}
async function readJson(request){let raw="";for await(const chunk of request)raw+=chunk;return JSON.parse(raw||"{}")}
const server=createServer(async(request,response)=>{try{if(request.url==="/health"){response.writeHead(200,{"content-type":"application/json"});response.end('{"status":"ok"}');return}if(request.url==="/api/item"&&request.method==="GET"){response.writeHead(200,{"content-type":"application/json"});response.end(JSON.stringify(await readState()));return}if(request.url==="/api/item"&&request.method==="POST"){const body=await readJson(request);const title=typeof body.title==="string"?body.title.trim():"";if(!title){response.writeHead(400,{"content-type":"application/json"});response.end('{"error":"title_required"}');return}await saveState({title});response.writeHead(200,{"content-type":"application/json"});response.end(JSON.stringify({title}));return}if(request.url==="/"||request.url==="/index.html"){response.writeHead(200,{"content-type":"text/html; charset=utf-8"});response.end(await readFile("dist/index.html","utf8"));return}response.writeHead(404);response.end("not found")}catch(error){response.writeHead(500,{"content-type":"application/json"});response.end(JSON.stringify({error:error instanceof Error?error.message:String(error)}))}});
server.listen(port,"127.0.0.1",()=>process.stdout.write(`fixture ready on ${port}\n`));const shutdown=()=>server.close(()=>process.exit(0));process.on("SIGTERM",shutdown);process.on("SIGINT",shutdown);
